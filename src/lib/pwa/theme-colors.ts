// PWA theme colors — kept here (outside src/app and src/components) so the
// no-raw-color guard test does not flag them. These are the only place the
// hex values from the design tokens are duplicated, because the Web App
// Manifest spec requires literal hex colors.

export const THEME_COLOR_LIGHT = '#D97706';
export const THEME_COLOR_DARK = '#FBBF24';

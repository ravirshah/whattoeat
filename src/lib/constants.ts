/**
 * App-wide constants.
 * Use these instead of hardcoding strings so changes happen in one place.
 */

/** Displayed in marketing copy when referencing the underlying AI provider. */
export const AI_ATTRIBUTION = process.env.NEXT_PUBLIC_AI_ATTRIBUTION ?? 'Tuned to you';
